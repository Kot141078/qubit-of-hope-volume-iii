# Source Provenance

Source location status: External local intake path removed for public portability.

The 28 edition files and one cover PNG were copied into the repository layout without modifying the source files. See `hashes/SHA256SUMS.source-tree.txt` for repository-relative protected asset hashes and `hashes/SHA256SUMS.repo-all.txt` for repository path hashes.

The cover file name was preserved because Volume II preserves its original cover file name.
